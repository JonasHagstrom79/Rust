{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/neo4J",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "MY_SECRET_KEY": "minhemlighet"
            },
            "preLaunchTask": "cargo build",
            "sourceLanguages": ["rust"]
        }
    ]
}